import { atom } from "recoil";

export const isShowgnbState = atom({
    key: "isShowgnb",
    default: true,// true:x false:o
})
export const isShowhamburgerState = atom({
    key: "isShowhamburgerState",
    default: true,
})
